/*
Q5. You're creating a basic login system to Make a login function with two things: a username and a
password. Check if the username is "admin" and the password is "12375". If they're both correct, show "Login
success full"; if not, show "Invalid credential'."
*/

Username="admin"
password="12375"

if(Username=="admin" && password=="12375"){
        console.log("login success full")
}
else{
    console.log("invalid credential")
}